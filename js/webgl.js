/* TABLE: webgl */

'use strict';

function outputWebGL() {
	let t0 = performance.now();
	// functions

	// perf
	let t1 = performance.now();
	outputDebug("1", "webgl", (t1-t0), (t1 - gt0));
};

//outputWebGL();
